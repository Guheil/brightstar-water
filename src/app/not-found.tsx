import NotFoundScreen from '@/screens/public/NotFoundScreen';

export default function RootNotFound() {
  return (
    <main id="main-content">
      <NotFoundScreen title="We could not find that page." />
    </main>
  );
}
